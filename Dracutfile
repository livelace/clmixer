#!/bin/bash

check() {
    require_binaries "$WORK_DIR/clmixer" || return 1
    return 255
}

depends() {
    echo "base"
    return 0
}

install() {
    # binaries.
    inst "$WORK_DIR/clmixer" "/usr/local/bin/clmixer"
}